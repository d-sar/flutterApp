import 'package:flutter/material.dart';

class ManitenaceManagement  extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Manitenace Management")),
      body: Center(child: Text("Manitenace Management")),
    );
  }
}
