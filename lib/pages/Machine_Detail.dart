import 'package:flutter/material.dart';

class MachineDetail  extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Machine Detail ")),
      body: Center(child: Text("Machine Detail")),
    );
  }
}
